lidar:
  -
    name: A                   # LiDAR Name  - A, B, C, D,
    topic: /rslidar_points_right     # LiDAR Topic
    frame: rslidar            # LiDAR Frame ID
    format: RSLIDAR_LEGACY           # LiDAR Format  - RSLIDAR, VELODYNE, LIVOX, OUSTER, SURESTAR, RS_PACKET_RSHELIOS_16P, RS_PACKET_RSHELIOS
    use: true                
    major: true
  -
    name: B
    topic: /rslidar_topic
    frame: rslidar
    format: RSLIDAR
    use: false
    major: false
    
imu:
  topic: /imu/data
  frame: imu
  use: true

wheel:
  topic: /wheel_odometry
  frame: wheel
  use: true

gnss: 
  topic: /gps
  frame: gps
  use: false